services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: deskflow
      POSTGRES_PASSWORD: deskflow
      POSTGRES_DB: deskflow
    ports: ["55432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U deskflow"]
      interval: 2s
      timeout: 3s
      retries: 20

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: deskify
      POSTGRES_PASSWORD: deskify
      POSTGRES_DB: deskify
    ports: ["55432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U deskify"]
      interval: 2s
      timeout: 3s
      retries: 20
